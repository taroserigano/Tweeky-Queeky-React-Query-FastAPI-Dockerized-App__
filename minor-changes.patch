diff --git a/.env.example b/.env.example
index 4d3de92..72a4d3b 100644
--- a/.env.example
+++ b/.env.example
@@ -1,5 +1,5 @@
 PORT=5000
-MONGO_URI=<MONGO_URL>
+MONGO_URI=mongodb://admin:adminpassword@mongodb:27017/tweeky?authSource=admin
 JWT_SECRET=your_jwt_secret_key_change_this_in_production
 PAYPAL_CLIENT_ID=your_paypal_client_id
 PAYPAL_APP_SECRET=your_paypal_secret
diff --git a/docker-compose.yml b/docker-compose.yml
index 75cd023..0e6ca73 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -3,23 +3,23 @@ version: "3.9"
 services:
   # MongoDB Atlas is used instead of local MongoDB
   # Local MongoDB service commented out for Atlas usage
-  # mongodb:
-  #   image: mongo:7.0
-  #   container_name: tweeky-mongodb
-  #   ports:
-  #     - "27017:27017"
-  #   environment:
-  #     - MONGO_INITDB_ROOT_USERNAME=admin
-  #     - MONGO_INITDB_ROOT_PASSWORD=adminpassword
-  #     - MONGO_INITDB_DATABASE=tweeky
-  #   volumes:
-  #     - mongodb_data:/data/db
-  #   restart: unless-stopped
-  #   healthcheck:
-  #     test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
-  #     interval: 10s
-  #     timeout: 5s
-  #     retries: 5
+  mongodb:
+    image: mongo:7.0
+    container_name: tweeky-mongodb
+    ports:
+      - "27017:27017"
+    environment:
+      - MONGO_INITDB_ROOT_USERNAME=admin
+      - MONGO_INITDB_ROOT_PASSWORD=adminpassword
+      - MONGO_INITDB_DATABASE=tweeky
+    volumes:
+      - mongodb_data:/data/db
+    restart: unless-stopped
+    healthcheck:
+      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
+      interval: 10s
+      timeout: 5s
+      retries: 5
 
   fastapi-backend:
     build: .
diff --git a/frontend/src/hooks/useOrderQueries.js b/frontend/src/hooks/useOrderQueries.js
index 9dc26f5..699ebea 100644
--- a/frontend/src/hooks/useOrderQueries.js
+++ b/frontend/src/hooks/useOrderQueries.js
@@ -1,5 +1,4 @@
-import { useQuery, useMutation, useQueryClient } from '@tantml:parameter>
-<parameter name="query">
+import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
 import axios from 'axios';
 import { BASE_URL, ORDERS_URL, PAYPAL_URL } from '../constants';
 
